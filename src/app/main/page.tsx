import { BioRoot } from '~/features/bio';
import { ProjectsRoot } from '~/features/projects';

const MainPage = () => {
  return (
    <main>
      <BioRoot />
      <ProjectsRoot />
    </main>
  );
};

export default MainPage;
