import Container from '~/components/Container';

const ProjectsOverviewTitle = () => {
  return (
    <Container className={''}>
      <h1 className={'text-4xl font-bold text-text-primary-red'}>Projects</h1>
    </Container>
  );
};

export default ProjectsOverviewTitle;
